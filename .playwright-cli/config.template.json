{
  "browser": {
    "browserName": "chromium",
    "launchOptions": {
      "headless": false,
      "args": [
        "--disable-extensions-except={{DIST_PATH}}",
        "--load-extension={{DIST_PATH}}"
      ]
    }
  }
}
